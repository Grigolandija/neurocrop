{
  "name": "neurocrop-backend",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:tenant": "node --test tests/tenant-isolation.integration.test.mjs tests/api-flows.integration.test.mjs",
    "migrate": "node migrate.js",
    "check": "node --check api.js && node --check ingest.js && node --check measurement-retention.js"
  },
  "dependencies": {
    "mqtt": "^5.3.0",
    "pg": "^8.11.3",
    "express": "^4.18.2",
    "cookie-parser": "^1.4.6"
  }
}
